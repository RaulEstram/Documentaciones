# Formas de iterar

Hay diferentes formas de iterar un elemento de esta API

* usando un foreach `for(x:y)`
* usando un iterator usando el metodo `.iterator()` para obejeter el iterator y posteriormente usar en un while el metodo `.hasNext()` sobre el Iterator
* usando el metodo `.forEach()`
* for normal en listas

